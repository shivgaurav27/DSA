//Inserting Elements
// Inserting a Single Element at a Specific Index

const arr = ['a', 'b', 'c'];

// splice(index,deleteCount,items which are coming to replace)

arr.splice(1, 0, 'D');

console.log(arr);
